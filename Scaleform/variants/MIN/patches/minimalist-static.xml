<?xml version="1.0" encoding="utf-8"?>
<actionScriptProfilePatch name="minimalist-static" version="1">
  <source path="venworks/cui/CUIRuntime.as">
    <replace>
      <find><![CDATA[   import Shared.AS3.Events.CustomEvent;
]]></find>
      <with><![CDATA[]]></with>
    </replace>
    <replace>
      <find><![CDATA[            valueContext = new CUIPlayerHudDataContext();
            valueContext.addEventListener(CUIPlayerHudDataContext.PROVIDER_ERROR,this.onProviderError);
            this.setDiagnosticContext("VALUE PROVIDER REGISTRATION",null,"TRANSACTIONAL START");
            valueContext.start();]]></find>
      <with><![CDATA[            valueContext = new CUIPlayerHudDataContext();]]></with>
    </replace>
    <replace>
      <find><![CDATA[            conditionContext = new CUIConditionContext();
            conditionContext.addEventListener(CUIConditionContext.PROVIDER_ERROR,this.onProviderError);
            this.setDiagnosticContext("CONDITION PROVIDER REGISTRATION",null,"TRANSACTIONAL START");
            conditionContext.start();
            conditionContext.addEventListener(CUIConditionContext.CONDITION_CHANGE,this.onConditionChanged);
            valueContext.addEventListener(CUIPlayerHudDataContext.VALUE_CHANGE,this.onValueChanged);
            valueContext.addEventListener(CUIPlayerHudDataContext.COMPASS_CHANGE,this.onCompassChanged);
            valueContext.addEventListener(CUIPlayerHudDataContext.TACTICAL_AWARENESS_CHANGE,this.onTacticalAwarenessChanged);]]></find>
      <with><![CDATA[            conditionContext = new CUIConditionContext();]]></with>
    </replace>
    <replace>
      <find><![CDATA[      private function onProviderError(param1:CustomEvent) : void
      {
         var details:Object = param1 == null ? null : param1.params;
         var lines:Array = [];
         if(this.disposed || this.teardownPending)
         {
            return;
         }
         if(details == null)
         {
            details = {
               code:"CUI-EVT-REPORTING",
               consumer:"UNKNOWN",
               provider:"UNKNOWN",
               message:"Provider error event did not contain diagnostic details."
            };
         }
         lines.push("CODE: " + String(details.code));
         lines.push("CONSUMER: " + String(details.consumer));
         lines.push("PROVIDER: " + String(details.provider));
         if(Number(details.errorID) != 0)
         {
            lines.push("ERROR ID: " + String(details.errorID));
         }
         if(details.message != null && String(details.message).length != 0)
         {
            lines.push(String(details.message));
         }
         if(details.stack != null && String(details.stack).length != 0)
         {
            lines.push("STACK: " + String(details.stack));
         }
         try
         {
            componentLayer.visible = false;
            diagnostics.showError("CUI EVENT HANDLER ERROR",lines.join("\n"));
         }
         catch(param2:Error)
         {
            trace("CUI-EVT-REPORTING | " + param2.toString());
         }
         this.deferComponentTeardown();
      }

]]></find>
      <with><![CDATA[]]></with>
    </replace>
    <replace>
      <find><![CDATA[      private function onConditionChanged(param1:CustomEvent) : void
      {
         try
         {
            this.setDiagnosticContext("LIVE VISIBILITY EVALUATION",null);
            this.applyConditions(param1.params);
            this.clearDiagnosticContext();
         }
         catch(param2:Error)
         {
            this.showLiveEventError(param2,"CONDITION CHANGE");
         }
      }

      private function onValueChanged(param1:CustomEvent) : void
      {
         try
         {
            this.setDiagnosticContext("LIVE VALUE EVALUATION",null);
            this.applyValues(param1.params);
            this.syncCriticalHealthCondition(param1.params);
            this.clearDiagnosticContext();
         }
         catch(param2:Error)
         {
            this.showLiveEventError(param2,"VALUE CHANGE");
         }
      }

      private function onCompassChanged(param1:Event) : void
      {
         try
         {
            this.setDiagnosticContext("LIVE CONTACT RADAR EVALUATION",null);
            this.applyContactRadars();
            this.clearDiagnosticContext();
         }
         catch(param2:Error)
         {
            this.showLiveEventError(param2,"COMPASS CHANGE");
         }
      }

      private function onTacticalAwarenessChanged(param1:Event) : void
      {
         try
         {
            this.setDiagnosticContext("LIVE TACTICAL AWARENESS EVALUATION",null);
            this.applyTacticalAwareness();
            this.clearDiagnosticContext();
         }
         catch(param2:Error)
         {
            this.showLiveEventError(param2,"TACTICAL AWARENESS CHANGE");
         }
      }

]]></find>
      <with><![CDATA[]]></with>
    </replace>
    <replace>
      <find><![CDATA[         if(conditionContext != null)
         {
            conditionContext.removeEventListener(CUIConditionContext.CONDITION_CHANGE,this.onConditionChanged);
            conditionContext.removeEventListener(CUIConditionContext.PROVIDER_ERROR,this.onProviderError);
            conditionContext.dispose();
         }
         if(valueContext != null)
         {
            valueContext.removeEventListener(CUIPlayerHudDataContext.VALUE_CHANGE,this.onValueChanged);
            valueContext.removeEventListener(CUIPlayerHudDataContext.COMPASS_CHANGE,this.onCompassChanged);
            valueContext.removeEventListener(CUIPlayerHudDataContext.TACTICAL_AWARENESS_CHANGE,this.onTacticalAwarenessChanged);
            valueContext.removeEventListener(CUIPlayerHudDataContext.PROVIDER_ERROR,this.onProviderError);
            valueContext.dispose();
         }]]></find>
      <with><![CDATA[         if(conditionContext != null)
         {
            conditionContext.dispose();
         }
         if(valueContext != null)
         {
            valueContext.dispose();
         }]]></with>
    </replace>
  </source>
  <source path="venworks/cui/components/CUIThreatAlert.as">
    <replace>
      <find><![CDATA[         background = new Shape();
         background.graphics.beginFill(backgroundColor,0.76);
         background.graphics.drawRect(0,0,componentWidth,componentHeight);
         background.graphics.endFill();
         addChild(background);]]></find>
      <with><![CDATA[         background = new Shape();
         background.graphics.lineStyle(1,clearColor,0.58);
         background.graphics.moveTo(0,8);
         background.graphics.lineTo(0,0);
         background.graphics.lineTo(32,0);
         background.graphics.moveTo(componentWidth - 32,0);
         background.graphics.lineTo(componentWidth,0);
         background.graphics.lineTo(componentWidth,8);
         background.graphics.moveTo(0,componentHeight - 8);
         background.graphics.lineTo(0,componentHeight);
         background.graphics.lineTo(32,componentHeight);
         background.graphics.moveTo(componentWidth - 32,componentHeight);
         background.graphics.lineTo(componentWidth,componentHeight);
         background.graphics.lineTo(componentWidth,componentHeight - 8);
         addChild(background);]]></with>
    </replace>
  </source>
  <source path="venworks/cui/components/CUIStatusEffectBar.as">
    <replace>
      <find><![CDATA[         param1.graphics.clear();
         param1.graphics.lineStyle(1,param4,0.54);
         param1.graphics.beginFill(backgroundColor,0.68);
         param1.graphics.drawRect(1,1,Math.max(0,param2 - 2),Math.max(0,param3 - 2));
         param1.graphics.endFill();]]></find>
      <with><![CDATA[         var corner:Number = Math.min(6,Math.max(2,param2 / 5));
         var right:Number = Math.max(1,param2 - 1);
         var bottom:Number = Math.max(1,param3 - 1);
         param1.graphics.clear();
         param1.graphics.beginFill(0x0D1114,0.28);
         param1.graphics.drawRect(1,1,Math.max(0,param2 - 2),Math.max(0,param3 - 2));
         param1.graphics.endFill();
         param1.graphics.beginFill(0x70CFE0,0.10);
         param1.graphics.drawRect(1,1,Math.max(0,param2 - 2),Math.max(0,param3 - 2));
         param1.graphics.endFill();
         param1.graphics.lineStyle(1,param4,0.54);
         param1.graphics.moveTo(1,corner);
         param1.graphics.lineTo(1,1);
         param1.graphics.lineTo(corner,1);
         param1.graphics.moveTo(right - corner,1);
         param1.graphics.lineTo(right,1);
         param1.graphics.lineTo(right,corner);
         param1.graphics.moveTo(1,bottom - corner);
         param1.graphics.lineTo(1,bottom);
         param1.graphics.lineTo(corner,bottom);
         param1.graphics.moveTo(right - corner,bottom);
         param1.graphics.lineTo(right,bottom);
         param1.graphics.lineTo(right,bottom - corner);]]></with>
    </replace>
  </source>
  <source path="venworks/cui/components/CUIScannerOverlay.as">
    <replace>
      <find><![CDATA[         var panelShape:Shape = new Shape();
         panelShape.graphics.lineStyle(1,scanningColor,0.86);
         panelShape.graphics.beginFill(backgroundColor,0.76);
         panelShape.graphics.drawRect(componentWidth / 2 - 112,0,224,26);
         panelShape.graphics.endFill();
         panelShape.graphics.lineStyle(1,contactColor,0.64);
         panelShape.graphics.beginFill(backgroundColor,0.72);
         panelShape.graphics.drawRect(componentWidth - 270,156,260,146);
         panelShape.graphics.endFill();
         addChild(panelShape);]]></find>
      <with><![CDATA[         var panelShape:Shape = new Shape();
         panelShape.graphics.beginFill(0x0D1114,0.28);
         panelShape.graphics.drawRect(componentWidth / 2 - 112,0,224,26);
         panelShape.graphics.drawRect(componentWidth - 270,156,260,146);
         panelShape.graphics.endFill();
         panelShape.graphics.beginFill(0x70CFE0,0.10);
         panelShape.graphics.drawRect(componentWidth / 2 - 112,0,224,26);
         panelShape.graphics.drawRect(componentWidth - 270,156,260,146);
         panelShape.graphics.endFill();
         this.drawCorners(panelShape,componentWidth / 2 - 112,0,224,26,scanningColor,0.86,18,7);
         this.drawCorners(panelShape,componentWidth - 270,156,260,146,contactColor,0.64,22,12);
         addChild(panelShape);]]></with>
    </replace>
    <replace>
      <find><![CDATA[      private function createContactFields() : void]]></find>
      <with><![CDATA[      private function drawCorners(param1:Shape, param2:Number, param3:Number, param4:Number, param5:Number,
         param6:uint, param7:Number, param8:Number, param9:Number) : void
      {
         var right:Number = param2 + param4;
         var bottom:Number = param3 + param5;
         param1.graphics.lineStyle(1,param6,param7);
         param1.graphics.moveTo(param2,param3 + param9);
         param1.graphics.lineTo(param2,param3);
         param1.graphics.lineTo(param2 + param8,param3);
         param1.graphics.moveTo(right - param8,param3);
         param1.graphics.lineTo(right,param3);
         param1.graphics.lineTo(right,param3 + param9);
         param1.graphics.moveTo(param2,bottom - param9);
         param1.graphics.lineTo(param2,bottom);
         param1.graphics.lineTo(param2 + param8,bottom);
         param1.graphics.moveTo(right - param8,bottom);
         param1.graphics.lineTo(right,bottom);
         param1.graphics.lineTo(right,bottom - param9);
      }

      private function createContactFields() : void]]></with>
    </replace>
  </source>
</actionScriptProfilePatch>
